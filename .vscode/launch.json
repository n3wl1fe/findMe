{
    "configurations": [
        {
            "name": "npm run dev",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "npm run dev"
        }
    ]
}